*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
build/
build_log.txt
# Signing — keep private! Never commit your keystore or its passwords.
*.keystore
keystore.properties
# Private guide (contains the signing password) — never upload
guide/
